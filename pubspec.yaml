name: chickenjoo_inventory
version: 1.0.0+1
publish_to: 'none'
description: "A new Flutter project."

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  drift: ^2.29.0
  sqlite3_flutter_libs: ^0.5.40
  path: ^1.9.0
  path_provider: ^2.1.5
  window_size:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  drift_dev: ^2.29.0
  build_runner: ^2.4.11

flutter:
  uses-material-design: true
  assets:
    - assets/images/chicken_joo_logo.png
